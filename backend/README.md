# Backend — Restaurant CRM

Placeholder for the future backend of the Restaurant CRM change (burger-page).

- **Status:** placeholder only — no implementation yet.
- **Planned scope:** persistence/API behind the storefront repository seam (see the `restaurant-crm` SDD design).
- The active application lives in [`../frontend/`](../frontend/).

Each folder keeps its own `package.json` — there are no npm workspaces.
