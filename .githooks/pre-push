#!/bin/sh
echo "Running typecheck and lint before push..."
npm run typecheck && npm run lint
if [ $? -ne 0 ]; then
  echo ""
  echo "Push aborted: fix type or lint errors before pushing."
  exit 1
fi
